[project]
name = "trim"
version = "0.1.0"

[project.scripts]
trim-video = "trim.cli:main"

[build-system]
requires = ["setuptools >= 35.0.2", "setuptools_scm >= 2.0.0, <3"]
build-backend = "setuptools.build_meta"
